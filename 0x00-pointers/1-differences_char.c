// 1. char A[50] allocates memory on the stack
// char* A = malloc(50) allocates memory on the heap.

// 2. The size of char A[50] is fixed at compile time while
// the size of char* A = malloc(50) can be changed at runtime.